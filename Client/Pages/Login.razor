@page "/"
@using Blazored.LocalStorage;
@inherits MainBase

@*@inject AuthenticationStateProvider AuthStateProvider
@inject HttpClient httpClient
@inject ILocalStorageService LocalStorage*@

        <!-- Tabs Titles -->
        <!-- Login Form -->
        <div class="wrapper">
            <div id="formContent">
        <h2 class="active"> Sign In </h2>
        <form @onsubmit=Logini>
            @*<div>
                <label for="username">Username </label>
                <input  id="username"  @bind="user.Username" name="login" />
            </div>
            <div>
                <label for="password">Password </label>
                <input id="password" @bind="user.Password" type="password" name="login" />
            </div>
                <button type="submit" class="btn btn-primary">Login</button>*@
            <input type="text" id="login" @bind="user.Username" name="login" placeholder="login">
            <input type="password" id="password" @bind="user.Password" name="login" placeholder="password">
            <div class="alert-danger">
                <span>@showError</span>
            </div>

            @*<input type="button"  @onclick="HandleLogin"  value="Log In">*@
            <button class="btn btn-primary shadow1">Login</button>
        </form>
    </div>
    
</div>
